@model Games
@{
    List<SelectListItem> realTeamsSelect = (List<SelectListItem>)ViewData["RealTeamsSelect"];
    List<SelectListItem> teamsSelect = (List<SelectListItem>)ViewData["TeamsSelect"];
    List<SelectListItem> wordsSelect = (List<SelectListItem>)ViewData["WordsSelect"];
    ViewData["Title"] = "Games Page";
}
<h2>Create Game</h2>


<form asp-action="Edit">
    <input asp-for="Id" type="hidden" />
    <div class="container-game">
        @Html.AntiForgeryToken()
        
        <div class="form-group">
            <label asp-for="StartTeamId" class="control-label">Start team</label>
            <select class="form-control" asp-for="StartTeamId" asp-items="@realTeamsSelect">
                <option>Please select one</option>
            </select>
            <span asp-validation-for="StartTeamId" class="text-danger"></span>
        </div>

        <div class="form-group">
            @for (int i = 0; i < 25; i += 5)
            {
                <div class="row">
                    <div class="col-lg-1"></div>
                    <div class="col-lg-2 word-guess">
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i).WordId" name="Gameswords[@(i)].WordId" asp-items="@wordsSelect">
                            <option>Please select one</option>
                        </select>
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i).TeamId" name="Gameswords[@(i)].TeamId" asp-items="@teamsSelect">
                            <option>Please select one</option>
                        </select>
                    </div>
                    <div class="col-lg-2 word-guess">
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 1).WordId" name="Gameswords[@(i + 1)].WordId" asp-items="@wordsSelect">
                            <option>Please select one</option>
                        </select>
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 1).TeamId" name="Gameswords[@(i + 1)].TeamId" asp-items="@teamsSelect">
                            <option>Please select one</option>
                        </select>
                    </div>
                    <div class="col-lg-2 word-guess">
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 2).WordId" name="Gameswords[@(i + 2)].WordId" asp-items="@wordsSelect">
                            <option>Please select one</option>
                        </select>
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 2).TeamId" name="Gameswords[@(i + 2)].TeamId" asp-items="@teamsSelect">
                            <option>Please select one</option>
                        </select>
                    </div>
                    <div class="col-lg-2 word-guess">
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 3).WordId" name="Gameswords[@(i + 3)].WordId" asp-items="@wordsSelect">
                            <option>Please select one</option>
                        </select>
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 3).TeamId" name="Gameswords[@(i + 3)].TeamId" asp-items="@teamsSelect">
                            <option>Please select one</option>
                        </select>
                    </div>
                    <div class="col-lg-2 word-guess">
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 4).WordId" name="Gameswords[@(i + 4)].WordId" asp-items="@wordsSelect">
                            <option>Please select one</option>
                        </select>
                        <select class="form-control" asp-for="@Model.Gameswords.ElementAt(i + 4).TeamId" name="Gameswords[@(i + 4)].TeamId" asp-items="@teamsSelect">
                            <option>Please select one</option>
                        </select>
                    </div>
                    <div class="col-lg-1"></div>
                </div>
            }
        </div>
    </div>

    <div class="form-group">
        <a class="btn btn-outline-primary" role="button" asp-area="" asp-controller="Games" asp-action="Index">Back to list</a>
        <input type="submit" value="Save" class="btn btn-outline-success" />
    </div>
</form>
